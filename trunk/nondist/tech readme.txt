To do music switching and the like

start a single song with 
	StartSong(string filename);
then switch with
	MusicSwitch(string filename, int speed);

all music must be in the /music directory

all sounds must be in the sounds directory